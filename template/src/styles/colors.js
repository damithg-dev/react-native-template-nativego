import { lightPalette , darkPalette } from './palette'
// import {useTheme} from '../utilities/context/theme';

// const {palette} = useTheme();

export const colors = {
    // mainBackgroundColor : isDark ? darkPalette.palette01 : lightPalette.palette01,
    // titleColor : isDark ? darkPalette.palette03 : lightPalette.palette03,
    // descriptionColor : isDark ? darkPalette.palette03 : lightPalette.palette03,
    // buttonBackgroundColor : isDark ? darkPalette.palette02 : lightPalette.palette02,
    // confirmButtonColor : isDark ? darkPalette.palette02 : lightPalette.palette02,
    // cancelButtonColor : isDark ? darkPalette.palette02 : lightPalette.palette02,
    // mainBackgroundColor : palette.palette01,
    // titleColor : palette.palette03,
    // descriptionColor : palette.palette03,
    // buttonBackgroundColor : palette.palette02,
    // confirmButtonColor :  palette.palette02,
    // cancelButtonColor : palette.palette02,


    mainBackgroundColor : lightPalette.palette01,
    titleColor : lightPalette.palette03,
    descriptionColor : lightPalette.palette03,
    buttonTitleColor : lightPalette.palette07,
    buttonDisableTitleColor : lightPalette.palette07,
    buttonBackgroundColor : lightPalette.palette02,
    buttonDisableBackgroundColor : lightPalette.palette02,
    confirmButtonColor :  lightPalette.palette02,
    cancelButtonColor : lightPalette.palette02,
    loadingIndicatorColor : lightPalette.palette02,
    overlayColor : lightPalette.palette05,
    imagePlaceholderColor : lightPalette.palette06,
    inputColor: lightPalette.palette03,
    inputPlaceholderColor : lightPalette.palette08,
    inputBackgroundColor : lightPalette.palette01,
    inputErrorColor : lightPalette.palette04,
    inputValidColor : lightPalette.palette02,
    inputFocusColor : lightPalette.palette02,
    inputNormalColor : lightPalette.palette03

}
